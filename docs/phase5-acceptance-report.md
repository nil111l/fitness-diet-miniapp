# 上线前最终验收报告

## 本地静态验收结果

- 小程序页面 JS 语法检查：通过。
- 云函数 JS 语法检查：通过。
- `app.json` 页面四件套检查：通过。
- WXML 坏闭合标签扫描：通过。
- 普通用户侧敏感信息扫描：已移除后台入口、平台密钥字段、会话密钥字段和技术 ID 文案。
- 管理后台接口校验：`admin` 云函数登录与所有管理接口均校验 `admin_users` 中的管理员身份。
- 用户私有数据隔离：饮食、运动、体重、统计、反馈、自定义食物均按云函数上下文 openid 过滤。
- 首页数据准确性：首页通过 `stats.dashboard` 从目标、饮食、运动、打卡集合实时汇总，不使用写死数据。
- 统计数据准确性：统计页通过 `stats.trends` 从近 7 天饮食、运动、体重集合汇总。

## 已修复问题

- 移除普通用户“我的”页中的管理后台入口。
- 移除 `admin_users` 为空时自动创建管理员的逻辑，避免首个访问者成为管理员。
- 修复禁用用户可重新登录的风险：`auth.login` 会拒绝 `disabled` 用户。
- 修复注销用户再次进入流程：注销用户重新登录会按新用户建档流程处理。
- 删除、禁用、下架等高风险操作补充二次确认。
- 隐私政策页改为用户可理解的说明，隐藏技术标识和密钥字段名。

## 必须真机或真实云环境验证

- 新用户登录、健康档案、目标设置。
- 老用户再次进入首页。
- 修改健康档案并检查推荐热量变化。
- 添加、编辑、删除饮食记录，并检查首页和统计页同步。
- 复制昨日饮食。
- 添加、删除运动记录，并检查首页运动消耗同步。
- 添加今日体重，并检查统计趋势。
- 提交反馈，并在后台查看。
- 后台新增、编辑、上架、下架、删除食材。
- 后台处理反馈状态和回复。
- 禁用用户后重新登录和调用受限功能。
- 真机首屏加载、食材搜索响应、图片反馈上传。

## 发布建议

1. 确认 10 个云函数均已“上传并部署：云端安装依赖”。
2. 在 `admin_users` 手动配置管理员 openid。
3. 在云开发控制台确认数据库集合权限不允许普通端直接读写私有集合，业务读写通过云函数完成。
4. 使用 `docs/test-accounts.md` 准备 5 类测试账号并跑完真机路径。
5. 确认 `project.config.json` 已忽略 quickstart 示例目录，`app.json` 未引用 quickstart 页面。
